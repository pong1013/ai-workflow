# Issue tracker: GitLab

This file records a truthful repository-level tracker choice for Matt-derived Skills.

## Repository identity

- **Repository:** `<confirmed-owner>/<confirmed-repository>`
- **Configured from remote:** `<confirmed-remote-name>`

This release of `$ai-workflow` cannot execute GitLab tracker operations. Stop at the pre-run Exception Gate before creating a Feature Run. Do not infer a GitHub target, create labels, assign or claim issues, close issues, or perform another tracker mutation from this configuration.

## Independent use

An independently invoked method may read GitLab issues only when the user supplies a concrete reference and appropriate tooling is available. Any write requires its own explicit authorization and is outside the bundled workflow.
