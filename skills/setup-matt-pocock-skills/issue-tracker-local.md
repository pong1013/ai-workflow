# Issue tracker: Local Markdown

This file records a truthful repository-level tracker choice for Matt-derived Skills.

## Location

- **Root:** `.scratch/`

This release of `$ai-workflow` cannot execute local-Markdown tracker operations. Stop at the pre-run Exception Gate before creating a Feature Run. Do not create ticket files, labels, assignments, claims, resolution state, or other tracker artifacts from this configuration.

## Independent use

An independently invoked method may read an explicitly supplied local specification. Any tracker write requires its own explicit authorization and is outside the bundled workflow.
