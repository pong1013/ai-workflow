interface:
  display_name: "Domain Modeling"
  short_description: "Build and sharpen repository domain knowledge"
  default_prompt: "Use $domain-modeling to sharpen this repository's terms and durable architecture decisions."
policy:
  allow_implicit_invocation: false
