interface:
  display_name: "To Tickets"
  short_description: "Publish approved tracer-bullet GitHub tickets"
  default_prompt: "Use $to-tickets to review and publish tracer-bullet tickets from this approved specification."
policy:
  allow_implicit_invocation: false
