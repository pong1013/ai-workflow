interface:
  display_name: "Grilling"
  short_description: "Stress-test thinking through design-tree questions"
  default_prompt: "Use $grilling to stress-test this plan until its consequential decision tree is settled."
policy:
  allow_implicit_invocation: false
