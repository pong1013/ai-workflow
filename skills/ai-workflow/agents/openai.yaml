interface:
  display_name: "AI Workflow"
  short_description: "Run a gated feature workflow through pull request"
  default_prompt: "Use $ai-workflow to run this feature through review and explicitly approved delivery."
policy:
  allow_implicit_invocation: false
